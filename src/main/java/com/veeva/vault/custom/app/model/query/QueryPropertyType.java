package com.veeva.vault.custom.app.model.query;

/**
 * Enum for Query Property field type, used in {@link QueryProperty} annotation
 */
public enum QueryPropertyType {
    BOOLEAN,
    INTEGER,
    BIG_INTEGER,
    DOUBLE,
    TEXT,
    LONG_TEXT,
    JSON
}
