package com.veeva.vault.custom.app.model.json;


/**
 * Enum for CSV Property Options, used in {@link JsonProperty} annotation
 */
public enum JsonPropertyOption {
    INSERT_NULL,
    IGNORE_ALL,
    IGNORE_NULL,
}
