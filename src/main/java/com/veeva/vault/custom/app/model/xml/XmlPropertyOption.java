package com.veeva.vault.custom.app.model.xml;

/**
 * Enum for XML Property Options, used in {@link XmlProperty} annotation
 */
public enum XmlPropertyOption {
    INSERT_NULL,
    IGNORE_ALL,
    IGNORE_NULL,
}
